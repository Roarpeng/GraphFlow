# GraphFlow 调研报告 v1.9.5（纵向 + 横向 + GraphFlow 自证）

> 调研日期：2026-08-02 ｜ 覆盖版本：`@roarpeng/graphflow@1.9.5`（此前综合报告覆盖至 v1.9.4）
> 方法：源码与 CHANGELOG 阅读、git 历史（250 commits）、GraphFlow 自身工具闭环（context / diagnose / skill_insights / plan-insight / index）、npm registry 实测、竞品联网核查（2026-08）

---

## 一、调研方法：GraphFlow 能力自证清单

| GraphFlow 工具 | 本次用途 | 产出 |
|---|---|---|
| `graphflow_context` | 全貌锚点压缩（53,659 raw → 282 tokens，**99% 节省**） | 7 摘要 + 7 锚点 |
| `graphflow_diagnose` | 运行时实况（图统计/运行统计/健康） | 3,246 节点 / 13,744 边 |
| `graphflow_skill_insights` | 技能库查询 | **0 技能（见 §五 关键发现）** |
| `graphflow_plan mode=insight` | 六顶思考帽 + 5-Why（deepseek 驱动） | 8 步 DAG 计划 + 帽子分析 |
| `graphflow_index` | 增量刷新图（本次 0 文件需重扫，索引已新鲜） | — |

## 二、项目现状快照（2026-08-01）

- **定位**：编码 Agent 的上下文与记忆层（README 自述，已从"编排引擎"收敛）
- **规模**：174 个 src TS 文件 / 约 47K LOC；95 测试文件 / **656 tests**；npm 39 版本（2026-06-11 发布，7 周历史）
- **分发**：npm 周下载 **1,077**（2026-07-25~31）、月下载 4,117 —— 认知度仍低，处于用户爬坡期
- **实时图状态**：3,246 节点（File 326 / Module 356 / Symbol 2,564）+ 13,744 边；600 次 context 运行，累计节省 **6.53M tokens**，平均节省 99%
- **健康**：5 provider 中 deepseek 健康（planner/worker/validator 全走 deepseek-v4）；compression/embedding backend = hash（离线默认）

## 三、纵向对比：7 周的版本演进

### 3.1 时间线

| 版本窗口 | 日期 | 主题 |
|---|---|---|
| v0.6.x | 06-10 → 06-14 | 建图与 MCP 骨架（22 工具时代） |
| v1.0.0 | 06-19 | 首个稳定版（280+ 测试） |
| v1.3.x | 06-28 → 07-03 | 压缩分层 L0-L3、存储后端抽象 |
| v1.4.x | 07-04 → 07-05 | 索引与压缩打磨 |
| v1.7.0 → 1.7.15 | 07-11 → 07-28（16 个版本） | 工具面 22→10 收敛、Bridge 委托、agent-delegated、词干匹配、PageRank LRU、HNSW 持久化、transport auto、golden set 26 查询、skill sync、飞轮报告、ATP/IR 公开规范 |
| v1.8.0 | 07-28 | **Goal 对齐**：goal 锚点节点化、低置信度澄清门、alignment-check、deviation 分类 |
| v1.9.0 | 08-01 | **技能质量与团队化**：语义 embedding 后端、技能四分类（proven/correctable/anti-pattern/noise）、golden 26→132、端到端 A/B（飞轮开 100% vs 关 61.5%）、CI 版本一致性门禁、skill sync 双向 merge、VS Code 飞轮面板、Graphify mcp-http 试点 |
| v1.9.1 | 08-01 | **记忆透明化**：记忆 ROI 基准（62 任务，ON 100% vs OFF 56.5%，归因链）、memory 审计 CLI、扩展改名 |
| v1.9.2→1.9.4 | 08-01 | Marketplace 身份修复 ×2、MCP 官方 SDK 迁移（协议 2024-11-05 → **2025-11-25**、logging/progress）、`graphflow://diagnose` MCP resource、README 重写 |
| **v1.9.5** | 08-01 | **索引卫生**：Agent 目录排除 + 批量剪枝（下详） |

### 3.2 投入方向迁移（纵向解读）

1. **能力搭建期（06 月）**：建图 → 压缩 → 22 个 MCP 工具 → 编排双轨
2. **分发打磨期（07 月）**：工具收敛到 10 个、15+ Agent 自动安装、Windows 坑、marketplace 发布 —— 精力在"让更多人装得上"
3. **证据补强期（07 底 → 08）**：golden set、A/B 基准（token/skill/memory 三套）、版本一致性 CI 门禁 —— 回应此前报告"飞轮缺乏效果验证"的批评，方向正确
4. **协议规范化（08-01）**：MCP SDK 化 + resources + ATP/IR 公开规范 —— 从"工具"走向"协议"

### 3.3 v1.9.5 delta 深挖（本次增量调研的核心）

- **问题**：`.claude/worktrees` 为每个 worktree 保存完整仓库副本，被当作源码索引进图 —— 本地实测 1,421 个 File 节点中 **1,076 个（76%）来自 worktrees**，污染检索锚点、PageRank 与存储。
- **修复一**：`IGNORED_DIRS` 新增 `.agent/.claude/.cursor/.gemini/.joycode/.trae/Cursor`，rebuild 扫描文件从 ~1,500+ 降至 **326**。
- **修复二（读写放大伴生问题）**：剪除 1,000+ 过期节点时，`pruneFileFromGraph` 逐节点 `deleteNode`（file 后端每次全量读写 JSON）导致 **62MB 图 × 1000+ 节点 → 305s 挂起**。新增可选能力 `GraphClient.deleteNodes(ids)`（file 单次读+写、sqlite 单事务分块、memory 批处理，均级联清边），剪枝改为"单次快照 + 单次批量删除"；mcp-http 保留逐节点回退。
- 测试 652 → **656**（+4）。

**纵向结论**：迭代极快（7 周 39 个 npm 版本、250 commits、单作者），且 v1.9.x 期间修正了大量此前报告指出的问题（测试时长、环境敏感断言、golden 回归、读写放大）。但节奏也暴露了**自修复型迭代**特征——大量修正是自己踩坑后补的（worktrees 污染、marketplace 重名、FTS camelCase），说明 dogfood 深度在增加，真实使用面在扩大。

## 四、横向对比：赛道全景（2026-08 联网核查）

### 4.1 分层地图

| 层级 | 代表 | 星数/规模 | 核心机制 | 对 GraphFlow 的含义 |
|---|---|---|---|---|
| **T1 图谱引擎** | CodeGraph | 47.4k★ / MIT | tree-sitter → SQLite FTS5，"一个 SQLite 文件"，watcher 增量 | 图谱单点最强、认知壁垒最高 |
| | GitNexus | 42k★ / PolyForm | 16 MCP 工具，自报省 88% 调用 | 商业许可 + 增量索引路线图缺失 |
| **T2 符号级** | Serena | 25.2k★ / MIT，v1.5.x 活跃 | **LSP-over-MCP**（40+ 语言），符号级编辑/重构，JetBrains 后端 | 符号操作事实标准，与 AST 图互补 |
| | claude-context | 11.8k★ | BM25 + 向量，默认发云 | 隐私对比点（GraphFlow local-first） |
| **T3 打包** | Repomix | 26.2k★ / ~25.5 万 npm 下载/月 | tree-sitter 压缩 ~70%，一次性打包 | 无状态 vs GraphFlow 按需召回 |
| | Aider repo-map | 内置 | tree-sitter + **PageRank** 仓库地图 | 与 GraphFlow L1 同算法族，已被验证有效 |
| **T4 记忆层** | claude-mem / Engram / Mem0 / SecureContext | 数千★ | **hook 驱动自动捕获**（SessionStart 注入、PreCompact 快照），FTS5/向量检索 | 直接竞争"跨会话记忆"，且它们更轻、捕获自动 |
| **平台内置** | Claude Code 原生 | — | CLAUDE.md + Auto Memory（静态注入）+ 自动压缩 | 最大的长期威胁（且是"免费"的） |
| **商业** | Augment（2.52 亿美元融资）/ Cody / Greptile | 闭源 | 云端 + 企业打磨 | 资源与完成度不可比 |

### 4.2 关键横向洞察

1. **赛道共识**：独立第三方 benchmark 是硬通货（CodeGraph ~70% 工具调用节省、grepai 97% token 均有独立数据）。GraphFlow 的 98.7% token 节省 / 飞轮 100% vs 56.5% 目前**全部是自测**。
2. **Serena 是补集而非敌手**：Serena 解决"精确改代码"（符号级），GraphFlow 解决"喂给 agent 前压缩 + 记忆"；docs/comparison.md 自己也承认"可叠加使用"。真正的竞争在 **T4 记忆层**：claude-mem/Engram 用 Claude Code hooks 自动捕获会话记忆（无需用户主动调用、压缩后也能注入）—— GraphFlow 的 episodic memory 需要 agent 主动调 `report_outcome`，捕获的自动性弱一档，但图结构化程度与项目级图谱深度强得多。
3. **平台内置化正在加速**：Claude Code 原生 Auto Memory（会话结束写 MEMORY.md、会话开始注入）已具雏形，但被业界评价为"静态 billboard"（无条件注入、不可检索、易腐化）；这正是 GraphFlow 这种"动态检索式记忆"的差异化窗口，且窗口会随时间收窄。
4. **Aider repo-map 验证了 GraphFlow L1 算法的方向**：tree-sitter + PageRank 的仓库地图是唯一经过大规模生产验证的压缩范式，GraphFlow 的 L1 图压缩与之同源。

## 五、GraphFlow 自证洞察（plan-insight 六顶帽结果）

| 帽子 | 结论 |
|---|---|
| **White** | GraphFlow 用 AST 级解析（TS 走 compiler API、其余 tree-sitter WASM），与 Serena 的 LSP 符号级是两种深度；文档类（Context7）与记忆类（claude-mem）竞品不做语法解析 |
| **Black** | 核心风险 = **噪声图**：边缺失/伪边/重构后过期会"误导" agent——**噪声图比无图更糟**（浪费 token 又侵蚀信任）；v1.9.5 快速迭代暗示图构建启发式仍可能不成熟 |
| **Yellow** | 复利护城河：图谱 + episodic 记忆 + 技能学习 + 压缩的**整合**，随使用时长增值，单点工具无法复制 |
| **Blue** | 事实优先：结论必须建立在图统计 + 技能库 + 检索质量数据之上 |

**本调研的 5-Why 根因（护城河验证缺口）**：
- 为什么飞轮价值存疑？→ 无生产效果证据。
- 为什么无证据？→ 飞轮闭环需要 agent 主动 report_outcome。
- 为什么没人回填？→ 本仓库 600 次 context 运行 **0 次 outcome 回填**、技能库 **0 skill、0 episode**——**GraphFlow 在自己的 dogfood 环境里从未闭环过飞轮**。
- 为什么没闭环？→ 闭环是"静默"的：compress/plan 无需用户配合即产生价值，outcome 回填则需显式协议动作。
- 根因 → **飞轮的最后一公里（agent 侧自动回填）缺失**，与 claude-mem/Engram 的 hook 自动捕获相比是结构性劣势。

## 六、风险与建议

### 风险（按严重度）

1. **护城河证据缺口**：飞轮机制最独特却最无生产证据；本仓库自用都没闭环（§五）。
2. **噪声图风险**（黑帽）：引用边靠正则扫描、符号哈希无冲突处理、PageRank 全图重算——大仓库下误导概率上升。
3. **单人维护 + 7 周 39 版本**：bus factor = 1；版本节奏正在放缓（07-28 后 5 天内 7 个版本均为修复/打磨），属于合理的减速但也是活力下降信号。
4. **平台内置化**：Claude Code/Codex 原生记忆成熟前，是 GraphFlow 的窗口期。

### 建议（按优先级）

| 优先级 | 建议 | 依据 |
|---|---|---|
| **P0** | **先把自己的飞轮转起来**：在 GraphFlow 仓库接入 hook 式 outcome 自动回填（或至少把 600 次 context 运行补上 outcome），让 diagnose 里出现真实 skill/episode | §五 根因：自己都没闭环，何谈用户闭环 |
| **P0** | **补 agent 侧自动捕获**：学习 claude-mem/Engram 的 hook 模式（SessionStart/PreCompact/PostToolFail 自动记录），把"主动调 report_outcome"降为可选 | §4.2 洞察 2 |
| **P1** | **独立 benchmark 外推**：公开 98.7%/100% vs 56.5% 的可复现方法（已有 infra），邀请第三方复现；hit-rate golden set（132 查询）已入 CI，方向对 | 赛道硬通货 |
| **P1** | **图噪声治理**：引用边质量（Trie/布隆）、符号哈希冲突、子图级 PageRank | 黑帽核心 |
| **P2** | **协议占位**：MCP resources 已上（graphflow://diagnose）；ATP/IR 已公开——保持协议层领先 | 平台内置化的对冲 |
| **P2** | **社区化**：open 贡献指南、issue 模板、路线图；缓解 bus factor | §六风险 3 |

## 七、一句话结论

GraphFlow 在 7 周内完成了"图谱 → 压缩 → 委托 → 记忆 → 证据"的五连跳，工程自愈力强（v1.9.5 自修 76% 污染节点），当前最大瓶颈不是功能而是**证明自己**：赛道等待独立 benchmark，用户等待记忆闭环，而它自己还没用起来自己的飞轮——从"让飞轮在自己的仓库先转起来"开始，是投入产出比最高的一步。
